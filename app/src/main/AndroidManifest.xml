<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eddardgao.mymvvmdemo01">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.Test07Activity"></activity>
        <activity android:name=".activity.Test06Activity" />
        <activity android:name=".activity.Test05Activity" />
        <activity android:name=".activity.Test04Activity" />
        <activity android:name=".activity.Test03Activity" />
        <activity android:name=".activity.Test02Activity" />
        <!--
        <receiver
            android:name=".broadcast.Test01DataBroadcastReceiver"
            android:enabled="true"
            android:exported="true" />
        -->
        <service
            android:name=".service.Test01DataService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".activity.Test01Activity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>